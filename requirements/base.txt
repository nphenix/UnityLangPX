# UnityLangPX 基础依赖
# 核心依赖
requests>=2.28.0
markdown>=3.4.0
aiohttp>=3.8.0
asyncio-throttle>=1.0.0
click>=8.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
toml>=0.10.0
loguru>=0.7.0
rich>=13.0.0

# HTTP服务器（用于CLI扩展，支持Obsidian插件）
# 注意：使用Python内置的http.server，无需额外依赖
# 如果需要更高级功能，可以考虑以下依赖：
# fastapi>=0.104.0
# uvicorn>=0.24.0

# 数据处理
numpy>=1.21.0
pandas>=1.3.0

# 数据库
# sqlite3 是Python标准库，无需安装

# 日志和监控
psutil>=5.8.0

# 文件处理
pathlib2>=2.3.0